<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#5AAFF2">

    <ImageView
        android:id="@+id/backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="fitXY" />

    <!-- The sample time baked into the chosen mock-up is covered by this live panel. -->
    <LinearLayout
        android:id="@+id/liveClockPanel"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:background="@drawable/live_clock_panel"
        android:gravity="end|center_vertical"
        android:orientation="vertical"
        android:paddingEnd="12dp">

        <TextClock
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:format12Hour="h:mm"
            android:format24Hour="HH:mm"
            android:gravity="end"
            android:shadowColor="#66000000"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="3"
            android:textColor="#FFFFFF"
            android:textSize="34sp"
            android:textStyle="bold" />

        <TextClock
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:format12Hour="yyyy. MMMM d., EEEE"
            android:format24Hour="yyyy. MMMM d., EEEE"
            android:gravity="end"
            android:shadowColor="#55000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Direct children of the FrameLayout: MainActivity sizes/positions them from the exact
         normalized card coordinates in the 1536x864 selected image. -->
    <View
        android:id="@+id/youtubeButton"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:background="@drawable/card_hotspot"
        android:clickable="true"
        android:contentDescription="YouTube"
        android:focusable="true" />

    <View
        android:id="@+id/disneyButton"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:background="@drawable/card_hotspot"
        android:clickable="true"
        android:contentDescription="Disney+"
        android:focusable="true" />

    <View
        android:id="@+id/netflixButton"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:background="@drawable/card_hotspot"
        android:clickable="true"
        android:contentDescription="Netflix"
        android:focusable="true" />

    <View
        android:id="@+id/maxButton"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:background="@drawable/card_hotspot"
        android:clickable="true"
        android:contentDescription="Max"
        android:focusable="true" />

    <View
        android:id="@+id/settingsButton"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:background="@drawable/settings_hotspot"
        android:clickable="true"
        android:contentDescription="@string/settings"
        android:focusable="true" />

</FrameLayout>
